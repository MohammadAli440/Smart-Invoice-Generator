# Smart Invoice Generator Web App

This workspace contains the frontend (Next.js) and backend (NestJS-styled) boilerplates for the Smart Invoice Generator project.

See the `frontend/` and `backend/` folders for details and start instructions.
