# Frontend (Next.js) - Smart Invoice Generator

Quick start:

1. Install dependencies: `npm install`
2. Start dev server: `npm run dev`

This app uses the App Router (Next 13+), TypeScript and Tailwind CSS.
